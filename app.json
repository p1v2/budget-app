{
  "environments": {
    "test": {
      "formation": {
          "test": {
            "quantity": 1,
            "size": "standart-1x"
          }
      },
      "scripts": {
        "addons":[
        "heroku-postgresql"
      ],
        "test-setup": "pip install -r requirements.txt",
        "test": "python manage.py test"
      }
    }
  }
}