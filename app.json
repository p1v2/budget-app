{
  "environments": {
    "test": {
      "scripts": {
        "addons":[
        "heroku-postgresql"
      ],
        "install-requirements": "pip install -r requirements.txt",
        "unit-tests": "python manage.py test"
      }
    }
  }
}